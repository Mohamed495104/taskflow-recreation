{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/graphql", "dest": "/api/graphql" },
     { "source": "/(.*)", "destination": "/api/graphql.js" }
  ]
}
